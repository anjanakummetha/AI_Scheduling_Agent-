# Timezone detection accuracy audit

Generated by `scripts/audit_timezone_accuracy.py`.

## Heidi workflow (implemented)

1. **Email chain** — signature block, explicit TZ mentions, city/state in reply text and quoted recipient segments (Kory/IFG blocks stripped).
2. **Area code** — phone numbers in signature/body (NANP → US zone).
3. **Prior threads** — same sender history in Lexi DB.
4. **Company domain** — known investor/firm map (`DOMAIN_TIMEZONE_HINTS`).
5. **Learned profile** — only trusted sources (domain, signature, area code).
6. **Fallback** — Lexi states uncertainty; times in **MT** with **(ET / CT / PT)** in one parenthetical.

## Latest results

| Suite | Result |
|-------|--------|
| Unit fixtures (`tests/test_timezone_intel.py`) | 18/18 pass |
| Offline audit fixtures | 6/6 pass |
| Live Kory inbox (scored) | 9/9 pass |

Re-run: `.venv/bin/python scripts/audit_timezone_accuracy.py`

Full JSON: `docs/TIMEZONE_ACCURACY_AUDIT.json`
